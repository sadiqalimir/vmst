// Add an event listener to the button
const button = document.querySelector('.button');
button.addEventListener('click', () => {
  // Show a message when the button is clicked
  alert('Thanks for downloading the tool!');
});
